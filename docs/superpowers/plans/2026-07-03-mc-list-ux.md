# Masterclass List UX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the current one-message-per-slot masterclass list with a single per-day message: one combined inline keyboard grouped by slot header, with title + spots-left on each button (place moved to the registration confirmation).

**Architecture:** Rewrite `handleMasterclasses` in `src/bot.ts` to build one `InlineKeyboard` across all of today's slots instead of sending a `ctx.reply` per slot. Add a no-op `mcnoop` callback for the inert slot-header buttons. Update `M.mcRegistered` to take a `place` argument and drop the now-unused `M.mcSlotTitle`/`M.mcLine` strings.

**Tech Stack:** TypeScript, grammY, googleapis (Google Sheets v4), Vercel serverless.

**Spec:** `docs/superpowers/specs/2026-07-03-mc-list-ux-design.md`

## Global Constraints

- No test framework and no local dev server: verify with `npm run typecheck` (must pass clean) plus diff inspection. Runtime behavior is confirmed after deploy (`npx vercel --prod` + `npm run set-webhook`), out of scope here.
- All user-facing strings are Ukrainian and live only in the `M` object in `src/messages.ts`.
- Callback data must stay under Telegram's 64-byte limit — the existing `Buffer.byteLength(cbData) > 64` skip in `handleMasterclasses` must be preserved.
- `mcreg:`/`mcunreg:` callback data format, `register`/`unregister` logic, and the `date !== todayISO()` stale-date guard are unchanged.
- Data-fetching stays exactly as-is: one `Promise.all` of `loadMasterclasses`, `loadMCSchedule`, `loadMCRegistrations` per invocation — this is a rendering change only, not a data-fetching change.

---

### Task 1: Combine the daily masterclass list into one message

**Files:**
- Modify: `src/messages.ts:15-32`
- Modify: `src/bot.ts:178-207` (`handleMasterclasses`)
- Modify: `src/bot.ts:242-265` (`mcreg:` callback — `M.mcRegistered` call sites)
- Modify: `src/bot.ts:267-275` (add `mcnoop` callback after the `mcunreg:` handler)

**Interfaces:**
- Consumes (unchanged): `loadMasterclasses`, `loadMCSchedule`, `loadMCRegistrations`, `todaySlots`, `activeRegs`, `register`, `unregister`, `Masterclass` (all already imported in `src/bot.ts`).
- Produces:
  - `M.mcDayTitle: string` — replaces `M.mcSlotTitle`/`M.mcLine` (removed; grep-confirmed no other consumers).
  - `M.mcRegistered(title: string, slot: string, place: string): string` — signature gains `place` (breaking change; both call sites updated in this same task since it's one Sheets-free rendering change).
  - Callback query `"mcnoop"` — new inert handler for slot-header buttons.

- [ ] **Step 1: Update `src/messages.ts`**

Replace lines 15-32:

```ts
  noMasterclassesToday: "Сьогодні майстер-класів немає.",
  mcSlotTitle: (slot: string) => `🎨 Майстер-класи ${slot}:`,
  mcLine: (
    mc: { title: string; place: string; capacity: number },
    taken: number,
    mine: boolean,
  ) =>
    `• ${mc.title} (${mc.place})${
      mc.capacity > 0 ? ` — ${taken}/${mc.capacity}` : ""
    }${mine ? " ✅" : ""}`,
  mcRegistered: (title: string, slot: string) =>
    `Ви зареєстровані на «${title}» (${slot}) ✅`,
  mcUnregistered: (title: string, slot: string) =>
    `Реєстрацію на «${title}» (${slot}) скасовано.`,
  mcFull: "На жаль, місць більше немає 😔",
  mcAlready: "Ви вже зареєстровані на цей майстер-клас.",
  mcSlotTaken:
    "У цей час ви вже зареєстровані на інший майстер-клас. Спершу скасуйте ту реєстрацію.",
```

with:

```ts
  noMasterclassesToday: "Сьогодні майстер-класів немає.",
  mcDayTitle: "🎨 Майстер-класи сьогодні:",
  mcRegistered: (title: string, slot: string, place: string) =>
    `Ви зареєстровані на «${title}» (${slot}, ${place}) ✅`,
  mcUnregistered: (title: string, slot: string) =>
    `Реєстрацію на «${title}» (${slot}) скасовано.`,
  mcFull: "На жаль, місць більше немає 😔",
  mcAlready: "Ви вже зареєстровані на цей майстер-клас.",
  mcSlotTaken:
    "У цей час ви вже зареєстровані на інший майстер-клас. Спершу скасуйте ту реєстрацію.",
```

- [ ] **Step 2: Rewrite `handleMasterclasses` in `src/bot.ts`**

Replace lines 178-207:

```ts
async function handleMasterclasses(ctx: Context) {
  const [mcs, schedule, regs] = await Promise.all([
    loadMasterclasses(),
    loadMCSchedule(),
    loadMCRegistrations(),
  ]);
  const slots = todaySlots(schedule);
  let sentAny = false;
  for (const s of slots) {
    const kb = new InlineKeyboard();
    const lines: string[] = [M.mcSlotTitle(s.slot), ""];
    let listed = 0;
    for (const id of s.mcIds) {
      const mc = mcs.find((m) => m.id === id);
      if (!mc) continue; // unknown ID in MCSchedule (or empty catalog) — skip silently
      const taken = activeRegs(regs, s.date, s.slot, mc.id);
      const mine = taken.some((r) => r.telegramId === String(ctx.from!.id));
      const cbData = `${mine ? "mcunreg" : "mcreg"}:${s.date}:${s.slot}:${mc.id}`;
      // Telegram rejects the whole message if any button's callback data exceeds 64 bytes
      if (Buffer.byteLength(cbData) > 64) continue;
      lines.push(M.mcLine(mc, taken.length, mine));
      kb.text(`${mine ? "❌" : "📝"} ${mc.title}`, cbData).row();
      listed++;
    }
    if (listed === 0) continue;
    await ctx.reply(lines.join("\n"), { reply_markup: kb });
    sentAny = true;
  }
  if (!sentAny) return ctx.reply(M.noMasterclassesToday);
}
```

with:

```ts
interface MCSlotButton {
  mc: Masterclass;
  taken: number;
  mine: boolean;
  cbData: string;
}

async function handleMasterclasses(ctx: Context) {
  const [mcs, schedule, regs] = await Promise.all([
    loadMasterclasses(),
    loadMCSchedule(),
    loadMCRegistrations(),
  ]);
  const slots = todaySlots(schedule);
  const kb = new InlineKeyboard();
  let anyListed = false;
  for (const s of slots) {
    const buttons: MCSlotButton[] = [];
    for (const id of s.mcIds) {
      const mc = mcs.find((m) => m.id === id);
      if (!mc) continue; // unknown ID in MCSchedule (or empty catalog) — skip silently
      const taken = activeRegs(regs, s.date, s.slot, mc.id);
      const mine = taken.some((r) => r.telegramId === String(ctx.from!.id));
      const cbData = `${mine ? "mcunreg" : "mcreg"}:${s.date}:${s.slot}:${mc.id}`;
      // Telegram rejects the whole message if any button's callback data exceeds 64 bytes
      if (Buffer.byteLength(cbData) > 64) continue;
      buttons.push({ mc, taken: taken.length, mine, cbData });
    }
    if (buttons.length === 0) continue;
    kb.text(`— ${s.slot} —`, "mcnoop").row();
    for (const b of buttons) {
      const label = `${b.mine ? "❌" : "📝"} ${b.mc.title}${
        b.mc.capacity > 0 ? ` — ${b.taken}/${b.mc.capacity}` : ""
      }`;
      kb.text(label, b.cbData).row();
    }
    anyListed = true;
  }
  if (!anyListed) return ctx.reply(M.noMasterclassesToday);
  return ctx.reply(M.mcDayTitle, { reply_markup: kb });
}
```

- [ ] **Step 3: Pass `place` at both `M.mcRegistered` call sites**

In the `mcreg:` callback (`src/bot.ts:242-265`), replace:

```ts
  const result = await register(date, slot, mcId, mc.capacity, ctx.from.id, me.name);
  await ctx.answerCallbackQuery(
    result === "ok"
      ? M.mcRegistered(mc.title, slot)
      : result === "full"
        ? M.mcFull
        : result === "already"
          ? M.mcAlready
          : M.mcSlotTaken,
  );
  if (result === "ok") await ctx.reply(M.mcRegistered(mc.title, slot));
  if (result === "slot_taken") await ctx.reply(M.mcSlotTaken);
});
```

with:

```ts
  const result = await register(date, slot, mcId, mc.capacity, ctx.from.id, me.name);
  await ctx.answerCallbackQuery(
    result === "ok"
      ? M.mcRegistered(mc.title, slot, mc.place)
      : result === "full"
        ? M.mcFull
        : result === "already"
          ? M.mcAlready
          : M.mcSlotTaken,
  );
  if (result === "ok") await ctx.reply(M.mcRegistered(mc.title, slot, mc.place));
  if (result === "slot_taken") await ctx.reply(M.mcSlotTaken);
});
```

- [ ] **Step 4: Add the `mcnoop` callback handler**

In `src/bot.ts`, immediately after the `mcunreg:` callback handler (ends at line 275 with `});`), add:

```ts

// Inert tap target for the slot-header row in the combined masterclass list.
bot.callbackQuery("mcnoop", (ctx) => ctx.answerCallbackQuery());
```

- [ ] **Step 5: Verify types**

Run: `npm run typecheck`
Expected: PASS, no errors.

- [ ] **Step 6: Verify no stale references remain**

Run: `grep -rn "mcSlotTitle\|mcLine\b" src/`
Expected: no output (both removed from `messages.ts` and their only call sites in `bot.ts` rewritten).

- [ ] **Step 7: Commit**

```bash
git add src/messages.ts src/bot.ts
git commit -m "feat: combine daily masterclass list into a single message"
```

---

## Self-Review

**Spec coverage:**
- One message per day, header text `M.mcDayTitle`, no per-item bullet list → Task 1 Step 2 ✓
- Slot header rows (`— 12:00-13:00 —`) as inert `mcnoop` buttons → Task 1 Steps 2, 4 ✓
- Buttons show title + spots-left, place dropped → Task 1 Step 2 (`label` construction) ✓
- Place moved to registration confirmation (`M.mcRegistered` gains `place`) → Task 1 Steps 1, 3 ✓
- Empty slot contributes no header/buttons; empty day → `M.noMasterclassesToday` → Task 1 Step 2 (`buttons.length === 0 continue`, `anyListed` check) ✓
- `M.mcSlotTitle`/`M.mcLine` removed → Task 1 Steps 1, 6 ✓
- Unaffected: `register`/`unregister`, callback data format, stale-date guard, `handleMyRegs` — none of these are touched by this plan ✓
- Data-fetching unchanged: single `Promise.all` retained in Step 2 rewrite ✓

**Placeholder scan:** none — every step has complete code; every verify step has an exact command and expected output.

**Type consistency:** `MCSlotButton` (new local interface, Task 1 Step 2) matches its only use site in the same step; `M.mcRegistered(title, slot, place)` signature (Step 1) matches both call sites (Step 3); `Masterclass` type reused from the existing `src/bot.ts` import (no new import needed, already present for `myOccurrencesToday`).
